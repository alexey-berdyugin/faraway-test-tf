apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-nginx-app.fullname" . }}
  labels:
    {{- include "test-nginx-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-nginx-app.selectorLabels" . | nindent 4 }}
